> [!success] Report
> - Change the session count updating request to send the number of completed sessions instead of indicating session was completed. (By doing that made the request idempotent.)
> - Change all the mqtt topics to have username at the beginning.
> - ESP32 will always publish with session count zero to retrieve back the state of the extension. Currently all state topic messages (Focus mode topic) has session count at the end.

---
# Idempotency
- Denoting an element of a set which is unchanged in value when multiple or otherwise operated on by itself.
- By following this principal I had change all the requests to have the same result in database in the same logic event.

