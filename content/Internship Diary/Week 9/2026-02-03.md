> [!success] Report
> - Total time method is transform into session based. Now total time is inferred by number of sessions completed.
> - On successful authentication from login page now result in the login page closing.
> - Fixed a bug related to the links not removing upon removing from the ui.

---
# Blocker
- **Browser based synchronization** ----> Currently I plan on each session complete it update the database. But the problem is if multiple browsers are open that would create unnecessary request. My suggestion is a priority based request handling. i.e For each new mqtt client join the server, the oldest members priority will be higher than the newest ones. The oldest member will publish the session complete request. But if two nodes join in the mqtt(two browsers) how would that behave? (Same priority may assign for each)
- If the user only use one browser no other ones + no hardware devices. He doesn't need the mqtt.
